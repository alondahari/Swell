.col-xs-10.col-xs-offset-1.rating-field
	h2.rating-header= header
	h2.rating-display
		span.rating-value= displayValue
		//- add space between value and unit
		| 
		| #{unit}
	input.rating-input-range(type="range", data-field=id)
	p.rating-time= time
